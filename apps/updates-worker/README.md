# Updates Worker

Cloudflare Worker starter for serving Expo OTA/update manifests through [`@expo-up/server`](https://www.npmjs.com/package/@expo-up/server).

## Commands

```bash
bun run dev
bun run deploy
bun run cf-typegen
```

Update the worker domain, project mapping, and GitHub credentials before using this in a real product.
